import { adminApp, adminAuth, adminFirestore } from './admin.firebase'
import { mainApp, mainAuth, mainDatabase, mainFirestore } from './main.firebase'

export { adminApp, adminAuth, adminFirestore, mainApp, mainAuth, mainDatabase, mainFirestore }
